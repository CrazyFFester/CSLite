using System;
using System.Collections.Generic;
using System.Linq;

namespace Aquarium
{
    class Program
    {
        private static void Main()
        {
            Fish[] fish = new Fish[0];
            AquariumInhabitants aquariumInhabitants = new AquariumInhabitants();

            while (true)
            {
                Console.SetCursorPosition(0, 18);
                aquariumInhabitants.ShowFishesInAnAquarium(fish);
                Console.SetCursorPosition(0, 0);

                aquariumInhabitants.FishConditionCheck(ref fish);
                СommandProcessing(ref fish, aquariumInhabitants);

                Console.ReadKey();
                Console.Clear();
            }
        }

        private static void СommandProcessing(ref Fish[] fish, AquariumInhabitants aquariumInhabitants)
        {
            int fishNumber;

            Console.WriteLine("Выберите команду:" +
            "\n1)Добавить рыбу" +
            "\n2)Достать рыбу");

            switch (Convert.ToInt32(Console.ReadLine()))
            {
                case 1:
                    aquariumInhabitants.AddFish(ref fish);
                    break;
                case 2:
                    Console.WriteLine("\nВвведите номер рыбы, которую хотите достать:");
                    fishNumber = Convert.ToInt32(Console.ReadLine()) - 1;
                    aquariumInhabitants.GetFishOutOfAquarium(ref fish, fishNumber);
                    break;
                default:
                    Console.WriteLine("Такой команды нет!!!");
                    break;
            }
        }
        class Fish : AquariumInhabitants
        {
            public Fish(int ageOfFish, int maximumAgeOfFish)
            {
                AgeOfFish = ageOfFish;
                MaximumAgeOfFish = maximumAgeOfFish;
            }
            public void ShowInfo(int i)
            {
                Console.WriteLine($"{i + 1}){AgeOfFish}/{MaximumAgeOfFish}");
            }
        }
        class AquariumInhabitants
        {
            protected int AgeOfFish;
            protected int MaximumAgeOfFish;

            public void AddFish(ref Fish[] fish)
            {
                Fish[] tempFish = new Fish[fish.Length + 1];
                tempFish[tempFish.Length - 1] = new Fish(0, 10);
                for (int i = 0; i < fish.Length; i++)
                {
                    tempFish[i] = fish[i];
                }
                fish = tempFish;
            }

            public void ShowFishesInAnAquarium(Fish[] fish)
            {
                Console.WriteLine("\nРыбы в аквариуме:");
                for (int i = 0; i < fish.Length; i++)
                {
                    fish[i].ShowInfo(i);
                    fish[i].AgeOfFish++;
                }
            }

            public void GetFishOutOfAquarium(ref Fish[] fish, int fishNumber)
            {
                for (int i = 0; i < fish.Length; i++)
                {
                    if (fishNumber == i)
                    {
                        List<Fish> tempFish = fish.ToList();
                        tempFish.RemoveAt(i);
                        fish = tempFish.ToArray();
                    }
                }
            }

            public void FishConditionCheck(ref Fish[] fish)
            {
                for (int i = 0; i < fish.Length; i++)
                {
                    if (fish[i].AgeOfFish == fish[i].MaximumAgeOfFish)
                    {
                        Console.WriteLine($"Одна из рыб умерла, она будет убрана из акавариума.\n");
                        GetFishOutOfAquarium(ref fish, i);
                    }
                }
            }
        }
    }
}
