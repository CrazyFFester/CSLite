using System;

namespace ReadInt
{
    class Program
    {
        static void Main(string[] args)
        {
            int userNumber = 0;

            Console.WriteLine(Conversion(userNumber));
            Console.ReadKey();
        }
        static int Conversion(int userNumber)
        {
            while (true)
            {
                Console.Write("Введите число: ");
                string userInput = Console.ReadLine();
                bool success = int.TryParse(userInput, out userNumber);
                if (success)
                {
                    return userNumber;
                }
                else
                {
                    ConsoleColor defualtColor = Console.ForegroundColor;
                    Console.ForegroundColor = ConsoleColor.Red;
                    Console.WriteLine("Ошибка!");
                    Console.ForegroundColor = defualtColor;
                }
            }
        }
    }
}
