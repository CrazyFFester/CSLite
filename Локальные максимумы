using System;

namespace Local_highs
{
    class Program
    {
        static void Main(string[] args)
        {
            Random randomValues = new Random();
            int[] arrayOfNumber = new int[30];
            int maxElement = int.MinValue;
            bool programState = true;
            Console.WriteLine("Изначальный массив:");

            for (int i = 0; i < arrayOfNumber.Length; i++)
            {
                arrayOfNumber[i] = randomValues.Next(0, 10);
                if (maxElement < arrayOfNumber[i])
                {
                    maxElement = arrayOfNumber[i];
                }
                Console.Write(arrayOfNumber[i] + " ");
            }
            while (programState == true)
            {
                Console.WriteLine($"\n\nМассив локальных маскимумов из {maxElement}:");
                for (int i = 0; i < arrayOfNumber.Length; i++)
                {
                    if (arrayOfNumber[i] == maxElement)
                    {
                        Console.Write(arrayOfNumber[i] + " ");
                    }
                    if (maxElement == 0)
                    {
                        programState = false;
                    }
                }
                maxElement--;
            }
            Console.ReadKey();
        }
    }
}
